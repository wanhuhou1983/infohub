# InfoHub Backend Configuration

# PostgreSQL
DATABASE_URL=postgres://infohub:infohub123@localhost:5433/infohub

# Server
PORT=3001

# Local file storage
DATA_DIR=./data

# EasyImages (image hosting) — 必填，无默认值
IMGBED_URL=http://localhost:8085/api/
IMGBED_BASE=http://localhost:8085
IMGBED_TOKEN=

# Miniflux (RSS)
MINIFLUX_URL=http://localhost:8084
MINIFLUX_USER=admin
MINIFLUX_PASS=miniflux123

# VPS Worker（国外 VPS，用于 YouTube 脏活 / Twitter 代理）
VPS_WORKER_URL=http://107.175.49.215:3100
WORKER_TOKEN=linhu50115
